# Local `bundle exec jekyll serve` with the github-pages gem works best on
# Ruby 3.1.x (not 3.2+ / 4.0). Use that toolchain for GH Pages parity.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"
